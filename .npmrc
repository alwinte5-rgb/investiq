# @clerk/clerk-expo transitively pulls Solana wallet adapters whose react-native
# peer ranges conflict with Expo SDK 51's pinned react-native. legacy-peer-deps
# lets the workspace install/hoist cleanly; the single RN version is enforced by
# the root "overrides" and the mobile Metro resolver.
legacy-peer-deps=true
